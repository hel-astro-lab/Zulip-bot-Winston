# Winston's secrets, one per line, told one per day in direct messages.
# Copy to ~/.config/winston/secrets.txt and replace these. The real file stays out of git.
The coffee machine on the second floor runs two degrees hotter than the thrid-floor machine.
Half of all referee reports are written on a Friday afternoon.
